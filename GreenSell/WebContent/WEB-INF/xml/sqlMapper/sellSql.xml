<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="sell">

	<select id="selectAll" resultType="com.greensell.sell.beans.ItemSellVO">
		select * from itemsell order by no desc
	</select>
	
	<select id="selectolditem" resultType="com.greensell.sell.beans.ItemSellVO">
		select * from itemsell where howsell=#{howsell} order by no desc
	</select>
  
  	<select id="selectauctionitem"  resultType="com.greensell.sell.beans.AuctionVO">
  		select i.*,a.startprice,a.nowprice,a.finishtime,a.tendernumber,a.nowemail
from itemsell i, auction a where i.no = a.itemno order by i.no desc
  	</select>
  
  	<delete id="deleteitem" parameterType="int">
  		delete from itemsell where no=#{no}
  	</delete>
  	
  	<delete id="deleteauction" parameterType="int">
  		delete from auction where no=(select no from auction where itemno=#{no})
  	</delete>
 
  	
  
</mapper>
